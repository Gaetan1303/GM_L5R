# Configuration serveur GameMaster L5R

# Port du serveur
PORT=3001

# Mode (development, production)
NODE_ENV=development

# URLs autorisées CORS (séparées par des virgules)
ALLOWED_ORIGINS=http://localhost:4200,http://localhost:3000,https://gaetan1303.github.io,https://gaetan1303.github.io/JDR-test

# Optionnel : URL de base de l'API (pour déploiement)
# API_BASE_URL=https://votre-backend-deploye.com
